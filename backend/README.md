# Backend Base

Node.js, Express, MongoDB, and Mongoose base API.

## Setup

```bash
npm install
copy .env.example .env
npm run dev
```

Default URL: `http://localhost:5000`

## Endpoints

- `GET /`
- `GET /api/health`
